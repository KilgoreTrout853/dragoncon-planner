# What scraper.py, tag_events.py and tests/ import beyond the standard
# library, pinned to the versions the pipeline was developed against
# (pip show, 2026-09-17, Python 3.13), plus pytest to run tests/.
# make_icons.py also needs Pillow; it is a one-off and not part of CI.
requests==2.32.5          # scraper.py, tag_events.py
beautifulsoup4==4.14.2    # scraper.py (bs4, with the stdlib html.parser)
urllib3==2.5.0            # scraper.py imports urllib3.util.retry directly
pytest==9.1.1             # python -m pytest tests/
