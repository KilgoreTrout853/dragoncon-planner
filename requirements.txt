# Every package the pipeline, its tests and the tag stage's API transport import beyond the standard library, and
# every package those need, each pinned, so the scrape workflow, CI and a hand run install the same files (DECISIONS
# #44: the workflow runs Python 3.13 from this file, as CI does). Pinned to the versions the pipeline was developed and
# tested against (pip, Python 3.13, 2026-09-23). Two install on Windows alone, by their markers. make_icons.py also
# needs Pillow; it is a one-off and not part of CI.
beautifulsoup4==4.14.2                     # scraper.py (bs4, with the stdlib html.parser)
certifi==2025.10.5                         # requests
charset-normalizer==3.4.3                  # requests
colorama==0.4.6 ; sys_platform == "win32"  # pytest, on Windows
ftfy==6.3.1                                # scraper.py: fix_encoding, the fetch's text repair (#44)
idna==3.11                                 # requests
iniconfig==2.3.0                           # pytest
packaging==26.3                            # pytest
pluggy==1.6.0                              # pytest
pygments==2.21.0                           # pytest
pytest==9.1.1                              # python -m pytest tests/
requests==2.32.5                           # scraper.py, tag_events.py
soupsieve==2.8                             # beautifulsoup4
typing_extensions==4.15.0                  # beautifulsoup4
tzdata==2025.2 ; sys_platform == "win32"   # zoneinfo on Windows: pipeline.py's season window
urllib3==2.5.0                             # requests; scraper.py imports urllib3.util.retry directly
wcwidth==0.8.4                             # ftfy
