# Secrets - never commit
.env
*.key
anthropic_key.txt

# Local backups from setup
*.bak

# Node (test deps only - index.html has no build step)
node_modules/

# Python
__pycache__/
*.pyc

# Build output (build.py); the next deploy publishes it, main never builds
site/
