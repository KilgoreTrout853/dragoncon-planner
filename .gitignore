# Secrets - never commit
.env
*.key
anthropic_key.txt

# Local backups from setup
*.bak

# Node
node_modules/

# Python
__pycache__/
*.pyc

# Build output (vite build); the next deploy publishes it, main never builds
dist/
